<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pe.edu.pucp.proyecto1_appocalipsis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SolicitudesDePrestamo"></activity>
        <activity android:name=".HistorialDePrestamo" />
        <activity android:name=".MenuPrincipalUsuario" />
        <activity android:name=".ReservaDispositivos" />
        <activity android:name=".MasDetalles" />
        <activity android:name=".ListarDispositivos" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>